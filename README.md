Web Interfaces for laydrop.com
===

Before doing anything run `bower install`. It will read the `bower.json` file and install all of the web dependencies from there.

Check the `.bowerrc` file for more options


